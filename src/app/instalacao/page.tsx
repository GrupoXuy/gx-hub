import InstallGuide from "@/components/install-guide";

export const metadata = {
  title: "GX Hub — Guia de publicação",
  description: "Guia oficial para validar credenciais e publicar o GX Hub.",
};

export default function InstallPage() {
  return <InstallGuide />;
}
